
[workspace]
resolver = "2"
members = [
    "actors",
    "bls-utils",
    "bls-unsafe",
    "bls-tests"
]

[profile.actor]
inherits = "release"
panic = "abort"
overflow-checks = true
lto = true
opt-level = "z"
#strip = true
